#
# Mednet EDC API
# 
# The Mednet EDC (Electronic Data Capture) REST API provides a single-point of access for reading data stored across iMednet data services. This specification documents all available endpoints in the latest version of the API. 
# The version of the OpenAPI document: 1.3.6
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables

import model_components_schemas_interval_forms_item

type Interval* = object
  ## 
  studyKey*: string ## Unique study key
  intervalId*: int ## Unique system identifier for the interval (visit definition)
  intervalName*: string ## Name of the interval (visit) as defined in the study
  intervalDescription*: string ## Description of the interval (visit)
  intervalSequence*: int ## Sequence number of the interval in the schedule
  intervalGroupId*: int ## Identifier for the interval group (if intervals are grouped)
  intervalGroupName*: string ## Name of the interval group
  timeline*: string ## Type of interval visit window (e.g., None, Due Date, Start - End Date, Actual Date)
  definedUsingInterval*: string ## Baseline interval used for calculating this interval’s dates
  windowCalculationForm*: string ## Baseline form (name) from which the calculation date is taken
  windowCalculationDate*: string ## Baseline field (variable name) from which the calculation date is taken
  actualDateForm*: string ## Form used to capture the actual date for this interval
  actualDate*: string ## Field (variable name) used to capture the actual date for this interval
  dueDateWillBeIn*: int ## Number of days from the calculation date when the interval is due
  negativeSlack*: int ## Number of days before the due date that are allowed (negative window)
  positiveSlack*: int ## Number of days after the due date that are allowed (positive window)
  eproGracePeriod*: int ## Number of days of grace period for ePRO completion after due date
  forms*: seq[components_schemas_Interval_forms_item] ## List of forms that are scheduled in this interval
  disabled*: bool ## Whether the interval is soft-deleted (disabled)
  dateCreated*: string ## Date when this interval was created
  dateModified*: string ## Date when this interval was last modified
