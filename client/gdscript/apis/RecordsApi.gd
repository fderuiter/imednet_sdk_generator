extends ApiBee
class_name RecordsApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API RecordsApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "RecordsApi"


# Operation createRecords → POST /studies/{studyKey}/records
# Add new record or update subject/record data
func create_records(
	# studyKey: String   Eg: studyKey_example
	# Study key identifying the study context for the request
	studyKey: String,
	# componentsSchemasRecordCreateRequestItem: Array
	# An array of record objects defining the operation to perform (subject registration, record creation, or scheduled record update).
	componentsSchemasRecordCreateRequestItem: Array,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/v1/edc/studies/{studyKey}/records".replace("{" + "studyKey" + "}", _bzz_urlize_path_param(studyKey))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "create_records.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = componentsSchemasRecordCreateRequestItem

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = RecordJobStatus.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func create_records_threaded(
	# studyKey: String   Eg: studyKey_example
	# Study key identifying the study context for the request
	studyKey: String,
	# componentsSchemasRecordCreateRequestItem: Array
	# An array of record objects defining the operation to perform (subject registration, record creation, or scheduled record update).
	componentsSchemasRecordCreateRequestItem: Array,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "create_records")
	bzz_callable.bind(
		studyKey,
		componentsSchemasRecordCreateRequestItem,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation listRecords → GET /studies/{studyKey}/records
# List records (eCRF instances) in a study
func list_records(
	# studyKey: String   Eg: studyKey_example
	# Study key identifying the study context for the request
	studyKey: String,
	# page: int = 0   Eg: 0
	# Page index to retrieve (0-based)
	page = 0,
	# size: int = 25   Eg: 25
	# Number of items to return per page (max 500)
	size = 25,
	# sort: String   Eg: sort_example
	# Sorting criteria in the format `property,ASC` or `property,DESC`. Can be repeated.
	sort = null,
	# filter: String   Eg: filter_example
	# Optional filter criteria to apply, in the format `attribute==value` with support for >, >=, <, <=, !=. Multiple criteria can be combined with `;` (AND) or `,` (OR).
	filter = null,
	# recordDataFilter: String   Eg: recordDataFilter_example
	# Optional filter criteria to apply on recordData fields (question responses). Supports ==, !=, <, <=, >, >=, =~ (contains) and can combine multiple criteria with `;` (AND) or `,` (OR).
	recordDataFilter = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `page` constraints
	if page < 0:
		var error := ApiError.new()
		error.identifier = "list_records.param.validation.minimum"
		error.message = "Invalid value for `page`, must be greater than or equal to 0."
		on_failure.call(error)
		return

	# Validate param `size` constraints
	if size > 500:
		var error := ApiError.new()
		error.identifier = "list_records.param.validation.maximum"
		error.message = "Invalid value for `size`, must be smaller than or equal to 500."
		on_failure.call(error)
		return
	if size < 1:
		var error := ApiError.new()
		error.identifier = "list_records.param.validation.minimum"
		error.message = "Invalid value for `size`, must be greater than or equal to 1."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/v1/edc/studies/{studyKey}/records".replace("{" + "studyKey" + "}", _bzz_urlize_path_param(studyKey))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["page"] = page
	bzz_query["size"] = size
	bzz_query["sort"] = sort
	bzz_query["filter"] = filter
	bzz_query["recordDataFilter"] = recordDataFilter

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = RecordList.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func list_records_threaded(
	# studyKey: String   Eg: studyKey_example
	# Study key identifying the study context for the request
	studyKey: String,
	# page: int = 0   Eg: 0
	# Page index to retrieve (0-based)
	page = 0,
	# size: int = 25   Eg: 25
	# Number of items to return per page (max 500)
	size = 25,
	# sort: String   Eg: sort_example
	# Sorting criteria in the format `property,ASC` or `property,DESC`. Can be repeated.
	sort = null,
	# filter: String   Eg: filter_example
	# Optional filter criteria to apply, in the format `attribute==value` with support for >, >=, <, <=, !=. Multiple criteria can be combined with `;` (AND) or `,` (OR).
	filter = null,
	# recordDataFilter: String   Eg: recordDataFilter_example
	# Optional filter criteria to apply on recordData fields (question responses). Supports ==, !=, <, <=, >, >=, =~ (contains) and can combine multiple criteria with `;` (AND) or `,` (OR).
	recordDataFilter = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "list_records")
	bzz_callable.bind(
		studyKey,
		page,
		size,
		sort,
		filter,
		recordDataFilter,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


